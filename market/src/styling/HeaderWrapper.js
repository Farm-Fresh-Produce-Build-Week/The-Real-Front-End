import styled from "styled-components";
import darkwheat from "../Images/darkwheat.jpg"; 

const HeaderWrapper = styled.div`
background-image: url(${darkwheat});
padding: .25rem;
margin-top: 3rem;
margin-bottom: 2rem 
`

export default HeaderWrapper; 